<% if Project.late.empty? %>
    <p>There are no past due Projects!</p>
<% else %>
    <% Project.late.each do |project| %>
        <%= link_to "#{project.title}", admin_project_path(project) %><br>
        <%= pretty_deadline(project) %>
    <% end %>
<% end %>