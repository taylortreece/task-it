<div id="container-fluid">
  <div class="row">
    <div class="col-lg" style="border:1px solid #ccc;overflow:auto;">
<h1><%= @project.title %></h1>
<%= link_to "Add Segments", admin_project_path(@project, :show_segment_form => true ) unless @segment %>&nbsp&nbsp
<%= link_to "Update Project", edit_admin_project_path(@project) %>&nbsp&nbsp
<%= link_to "Delete Project", admin_project_path(@project), :method => :delete %><br><br>
    <div class="col-content">
<%= pretty_deadline(@project) %><br>
<%= @project.description %><br><br>

<%= render :partial => "admin/projects/completed_form" %><br>

<h3>Segments:</h3>
<%= render :partial => "admin/projects/show_segments" %>

<%= render :partial => 'admin/segments/form', :locals => { :segment => @segment } if @segment %><br>

        </div>
     </div>
   </div>
</div>
<%= render :partial => 'admin/project_comments/form', :locals => { :subject => @project } %>

<br><%= render :partial => "admin/projects/show_comments" %>
